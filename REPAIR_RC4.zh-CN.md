# 0.4.2-rc.4：实时消息显示、共同记忆与失败续跑

本版本为预发布版。包含自 0.4.1 以来的工具输入、失败续跑及前端显示修复；安装包已包含构建产物，可直接安装。

## 消息与前端

- 修复新消息尚未写入插件状态时，空 ID 被判为同一消息而导致整个消息组件崩溃的问题。用户消息、正文和角色卡组件不再因此需要刷新才出现。
- 正文和用户输入先保存，再执行表格与记忆更新。后续工具失败时正文仍保留，下一轮可以继续读取。
- 连接恢复时补读最新状态；生成期间的状态通知不覆盖脚本草稿；组件更新会清理过时的加载回调。
- 思考内容可以展开、复制。写作只返回思考时保留原始响应并明确报错，不自动把思考当成正文。

## 共同记忆与工具请求

- 将兼容模型配置的角色合为一次记忆抽取。公共事件正文存一份，各角色档案保存知情引用和个人状态变化。
- 私人信息有明确知情范围；独立召回只读取该角色可见的事件，汇总给推进和写作时共享事件只发送一次。
- 旧记忆保持可读；full 兼容模式及不同角色模型配置保留独立提取路径。角色人数变为单人时，旧共同事件引用仍可读取。
- 已有属性可按同一角色档案内完全相同的 subject/key 本地解析，避免仅为补内部 ID 再请求模型。未知 ID、无效证据和重复更新仍拒绝。
- 结构化工具请求连续 60 秒无数据或单次超过 180 秒时停止。超时、长时间运行后的传输中断不自动重放；短暂连接错误仍遵循提供方有限重试策略。

## 其它累计修复

- 成功阶段保存检查点；失败或重启后只补未完成阶段，已生成正文无需重写。改变输入、来源或配置会使对应旧结果失效。
- 导入推进预设时按原任务执行；工具采用与职责相符的输入。固定提示词和稳定 schema 在前，历史与变量等动态资料在后。
- 结构错误最多进行一次局部修复；可无损转换的字符串数组在本地处理。拒绝、永久错误和输出截断不会反复重发大请求。
- 补充失败请求的用量、耗时、部分思考与工具参数记录。用量来自提供方，不把本地字符估算当成计费 token。
- 修复 Node 权限模式下 hoisted 依赖的模板读取权限，保留原文件访问边界。

## 安装与升级

需要 Node.js >=26.4 和已配置的 DSH Web。当前版本实测环境为 macOS arm64、Node.js 26.8.2、DSH 0.1.5-rc.1、pnpm 11.21.0。

先停止正在运行的 dsh web，再执行 README 的安装命令；无需卸载已有插件。安装完成后重新启动 dsh web，并刷新已经打开的页面以载入新版前端。

## 验证与边界

- 284 项自动测试通过，语法检查及前端构建通过。
- 使用实际 CMD / DeepSeek V4.1 Flash，连续两轮在不刷新页面的情况下验证用户消息、正文与协议组件显示；共享事件及私人事件归属通过检查。
- 发布包的 src、lib 与本机已验证版本逐文件核对一致。
- 这是候选版本，尚未覆盖 Windows。旧历史记忆没有自动进行语义合并。
- 长预设和提供方思考输出仍可能产生较高消耗；本版本不承诺具体 token 降幅或缓存命中率，也没有把未生效的思考关闭选项作为修复。
