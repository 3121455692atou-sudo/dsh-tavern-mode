# 0.4.2-rc.6：从失败步骤继续，按无输出时间判定超时

rc.4 的工具请求有 180 秒总时限，外层工具另有 10 分钟时限，会中断仍在流式输出的请求。本版本移除这两个总时限：正文、思考或工具参数持续有新内容就继续；连续 60 秒没有新内容才停止。空增量、用量事件和连接心跳不算新内容。手动停止仍有效，失败时保留已收到的原始输出。

之前的重试入口仅在正文已生成、表格或记忆失败时出现。本版本在输入框上方提供「从失败步骤继续」按钮，识别角色、召回、场景组合、推进、写作失败时都可使用。按钮显示失败阶段和已保存步骤数；恢复时读取磁盘检查点，复用成功结果，只请求未完成步骤及必要后续任务。

继续操作保留原用户输入及其消息关联，不把控制命令写进剧情，也不再次触发发送消息的脚本回调。正文已经保存时，只补表格和记忆更新。输入、配置或来源改变后不静默重跑成功步骤，会明确报告无法安全复用。

旧版检查点从 DSH 原生日志恢复用户消息及失败阶段。原生日志位置不再被误判为剧情输入变化；旧版前端没有保存的运行快照，改用每个成功请求的精确哈希逐步核验，全部成功请求匹配后才允许新的模型调用，避免因宿主元数据变化要求整轮重跑。

普通模式切换只提交玩法变更，后端合并已有配置并补齐默认字段，保留模型与并发设置；无角色卡的会话也能切换玩法。开场界面跟随酒馆消息记录显示，不再因宿主的空会话标记变化而消失。

初次安装和更新使用同一条命令：

```bash
dsh plugin --profile web add --allow-build=esbuild "https://github.com/3121455692atou-sudo/dsh-tavern-mode/releases/download/v0.4.2-rc.6/dsh-tavern-mode-0.4.2-rc.6.tgz"
```

先停止 dsh web，安装后重新启动并刷新网页。

验证：303 项自动测试通过，覆盖不同阶段失败后续跑、跨重启复用、原输入保持、按钮交互、手动停止与流式超时。虚拟时间测试覆盖持续输出 15 分钟不被总时限截断，以及空心跳无法延长无输出时限。
