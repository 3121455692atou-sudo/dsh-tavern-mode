# 0.4.2-rc.8：自动纠错次数按设置执行

此前界面允许设置 0–3 次自动纠错，但后端使用“是否已纠错”的布尔值，导致 1、2、3 实际都最多纠错一次。本版改为按次数计数。

## 重试行为

| 设置 | 首次请求后的额外纠错次数 | 最多协议尝试次数 |
|---|---|---|
| 0 | 0 | 1 |
| 1 | 1 | 2 |
| 2 | 2 | 3 |
| 3 | 3 | 4 |

- 工具 agent 和导入的推进任务都使用此设置，每项任务独立计数；结果通过校验即停止。
- 每次纠错只发送最新候选结果、校验错误和必要资料。固定协议与工具定义保持在前，不累积失败历史，也不重发最初的整份提示词。
- 拒答、没有可修复结果、输出截断、静默超时和手动取消仍会停止。网络错误另遵循提供方的重试策略，不重置或消耗自动纠错计数。
- 每次尝试的原始响应、状态和用量继续保存；已成功的其他任务不会因本次纠错重新运行。
- 继续保留 rc.7 的输出策略：插件不增加输出 token 上限，持续输出不按总时长截断，正文目标字数只作提示。

## 验证

回归覆盖设置 0、1、2、3 的次数边界，最后一次纠错成功、提前成功、重试耗尽、传输失败与纠错交错、手动取消、推进预设和原生错误记录。纠错期间仍检查字段类型、角色归属及原始资料，拒答和截断不会触发剩余纠错。

## 安装或更新

停止 `dsh web` 后执行，完成后重新启动并刷新页面：

```bash
dsh plugin --profile web add --allow-build=esbuild "https://github.com/3121455692atou-sudo/dsh-tavern-mode/releases/download/v0.4.0/dsh-tavern-mode-0.4.0.tgz"
```

固定链接的路径和文件名保留 `0.4.0`，包内版本为 `0.4.2-rc.8`。本版也提供独立下载地址：

```bash
dsh plugin --profile web add --allow-build=esbuild "https://github.com/3121455692atou-sudo/dsh-tavern-mode/releases/download/v0.4.2-rc.8/dsh-tavern-mode-0.4.2-rc.8.tgz"
```
